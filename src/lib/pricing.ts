export const LISTING_MONTHLY_PRICE = 150
export const LISTING_PRICE_LABEL = "$150 per month"
export const LISTING_PRICE_SHORT = "$150/mo"

export function listingPriceCopy() {
  return `${LISTING_PRICE_LABEL} for a PlaceFind business listing.`
}
